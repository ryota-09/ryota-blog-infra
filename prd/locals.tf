# AWS
locals {
  aws_region = var.aws_region
}

locals {
  repo_name = var.repo_name
}

locals {
  env_name = var.env_name
}

locals {
  repo_org = var.repo_org
}

locals {
  apprunner_role_arn = var.apprunner_role_arn
}

locals {
  image_uri = var.image_uri
}